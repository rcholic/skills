{
  "id": "moltguard",
  "name": "MoltGuard",
  "description": "API-based prompt injection detection powered by MoltGuard",
  "version": "5.0.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "Enable or disable the guard (default: true)"
      },
      "blockOnRisk": {
        "type": "boolean",
        "description": "Block tool calls when injection is detected (default: true)"
      },
      "apiKey": {
        "type": "string",
        "description": "MoltGuard API key (optional — auto-registered if missing)"
      },
      "timeoutMs": {
        "type": "number",
        "description": "Timeout for analysis in milliseconds (default: 60000)"
      },
      "dbPath": {
        "type": "string",
        "description": "Path to SQLite database for logging (default: ~/.openclaw/openclawguard.db)"
      }
    }
  },
  "uiHints": {
    "enabled": {
      "label": "Enable Guard",
      "help": "Enable or disable prompt injection detection"
    },
    "blockOnRisk": {
      "label": "Block on Risk",
      "help": "Block tool calls when prompt injection is detected"
    },
    "apiKey": {
      "label": "API Key",
      "help": "Your MoltGuard API key. Leave blank for automatic registration on first use.",
      "advanced": true
    },
    "timeoutMs": {
      "label": "Timeout (ms)",
      "help": "Maximum time for analysis",
      "advanced": true
    },
    "dbPath": {
      "label": "Database Path",
      "help": "Custom path for the analysis log database",
      "advanced": true
    }
  }
}
